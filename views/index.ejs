<%- include('partials/header.ejs') %>
<%- include('partials/nav.ejs') %>

<ul>
    <% if (recipes && recipes.length) { %>
        <% recipes.forEach(recipe => { %>
            <li><%= recipe.title %> - <%= recipe.cuisine %></li>
        <% }); %>
    <% } else { %>
        <li>No recipes found.</li>
    <% } %>
</ul>

<%- include('partials/footer.ejs') %>
