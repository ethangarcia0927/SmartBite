<%- include('partials/header.ejs') %>
<%- include('partials/nav.ejs') %>



<div class="mainBox">
    <h1>Recipes Found</h1>

    <% if (recipes.length === 0) { %>
        <p>No recipes found matching your search.</p>
    <% } else { %>
        <p><%= recipes.length %> recipe(s) found</p>
        
        <div class="recipe-list">
            <% for (let recipe of recipes) { %>
                <div class="recipe-card">
                    <img src="<%= recipe.img_url %>" alt="<%= recipe.title %>">
                    <h2><%= recipe.title %></h2>
                    <p><strong>🍽️ Cuisine:</strong> <%= recipe.cuisine %></p>
                    <p><strong>🍴 Meal Type:</strong> <%= recipe.meal_type %></p>
                    <p><strong>🥗 Diet:</strong> <%= recipe.diet %></p>
                    <p><strong>Budget:</strong> <%= recipe.budget_level %></p>
                    <p><strong>💲 Price:</strong> $<%= recipe.price ? parseFloat(recipe.price).toFixed(2) : '0.00' %></p>
                    <p><strong>⏱️ Cook Time:</strong> <%= recipe.cook_time %> minutes</p>
                    
                    <div class="nutrition">
                        <span>Fat: <%= recipe.fat %>g</span>
                        <span>Carbs: <%= recipe.carb %>g</span>
                        <span>Protein: <%= recipe.protein %>g</span>
                    </div>
                    
                    <p><strong>Ingredients:</strong> <%= recipe.ingredients %></p>
                    <p><strong>Instructions:</strong> <%= recipe.instructions %></p>
                </div>
            <% } %>
        </div>
    <% } %>

    <a href="/" class="btn btn-light back-btn">← Back to Search</a>


</div>

<%- include('partials/footer.ejs') %>
